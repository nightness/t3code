{
  "name": "@t3tools/capacitor",
  "private": true,
  "type": "module",
  "scripts": {
    "web:copy": "node scripts/copy-web.mjs",
    "sync": "pnpm web:copy && cap sync",
    "sync:ios": "pnpm web:copy && cap sync ios",
    "sync:android": "pnpm web:copy && cap sync android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/app": "8.1.1",
    "@capacitor/core": "8.5.2",
    "@capacitor/ios": "8.5.2",
    "@capacitor/keyboard": "8.0.5"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
